// export const reduceItemsInStock = async (items: { id: string, qty: number, countInStock: number }[]) => {
//     newCountInStock = items.map(item => item.countInStock - item.qty)
//     //implement newCountInStock => backend => db
//   }